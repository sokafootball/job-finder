// import { updateUserInput } from '../userInput/userInputSlice'
// import { userInputEpic } from './userInputEpic'

// describe('userInputEpic', () => {
//   it(`should only handle actions of type ${updateUserInput.toString()}`, () => {
//     expect(userInputEpic({ type: 'wrong type' }).toEqual())
//   })
// })
describe('userInputEpic', () => {
  it(`placeholder test`, () => {})
})
